# WeathermapNG Config Security
# Deny all access to configuration files

<RequireAll>
    Require all denied
</RequireAll>

# Allow access only from localhost/internal network for API calls
<RequireAny>
    Require local
    Require ip 127.0.0.1
    Require ip ::1
    Require ip 192.168
    Require ip 10
    Require ip 172.16/12
</RequireAny>

# Prevent directory listing
Options -Indexes

# Disable PHP execution in this directory
<Files *.php>
    Require all denied
</Files>

# Prevent access to sensitive files
<FilesMatch "\.(conf|ini|env|key)$">
    Require all denied
</FilesMatch>